{
  "name": "testimonialgrid",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tailwindcss -i ./input.css -o ./css/styled.css",
    "watch": "npx tailwindcss -i ./input.css -o ./css/styled.css --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tailwindcss": "^3.1.5"
  },
  "devDependencies": {
    "@tailwindcss/line-clamp": "^0.4.0"
  }
}
